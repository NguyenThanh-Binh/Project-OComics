import { jsx as _jsx } from "react/jsx-runtime";
import { render } from '@testing-library/react';
import { ProductCardFooter } from './product-card-footer';
describe('<ProductCardFooter>', () => {
    test('it should render properly', () => {
        const { container } = render(_jsx(ProductCardFooter, {}));
        expect(container).not.toBeNull();
    });
    test('it should render properly with props', () => {
        const { container } = render(_jsx(ProductCardFooter, { isLoading: true, children: _jsx("div", { children: "some content here" }) }));
        expect(container).not.toBeNull();
    });
});
